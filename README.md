# 🚀 Bank Cards Management System

Backend-приложение на Java (Spring Boot) для управления банковскими картами, реализованное в рамках тестового задания.

## 🛠 Технологический стек

*   **Java 17**
*   **Spring Boot 3.5.3**
*   **Spring Security + JWT** (Аутентификация и авторизация)
*   **Spring Data JPA** (Работа с БД)
*   **PostgreSQL**
*   **Liquibase** (Миграции базы данных)
*   **Docker & Docker Compose**
*   **Swagger (OpenAPI 3.0)** (Документация)
*   **Lombok & MapStruct**

## 📋 Функциональные возможности

### Для Администратора:
*   Управление пользователями (просмотр, блокировка, активация).
*   Управление картами (создание для счета, блокировка, активация, удаление).
*   Просмотр всех карт в системе.

### Для Пользователя:
*   Регистрация и вход.
*   Автоматическое создание банковского счета при регистрации.
*   Просмотр своих карт с поддержкой пагинации и поиска по маскированному номеру (PAN).
*   Запрос блокировки своей карты.
*   Переводы между своими счетами.
*   Просмотр баланса.

## 🔒 Безопасность
*   **JWT**: Доступ к защищенным эндпоинтам осуществляется через Bearer токен.
*   **Шифрование**: Номера карт (PAN) хранятся в базе данных в зашифрованном виде.
*   **Маскирование**: В API номера карт передаются в маскированном формате (например, `**** **** **** 1234`).

## 🚀 Запуск приложения

### Предварительные требования
*   Установленный **Docker** и **Docker Compose**.
*   (Опционально) **Java 17** и **Maven**, если планируется запуск без Docker.

### 1. Запуск через Docker Compose (Рекомендуется)

Самый быстрый способ запустить приложение вместе с базой данных:

```bash
docker-compose up -d
```

Приложение будет доступно по адресу: `http://localhost:8080`

### 2. Запуск локально (для разработки)

1.  Запустите только базу данных PostgreSQL:
    ```bash
    docker-compose up -d postgres
    ```
2.  Запустите Spring Boot приложение:
    ```bash
    ./mvnw spring-boot:run
    ```

## 📖 Документация API

После запуска приложения документация Swagger UI доступна по адресу:
`http://localhost:8080/swagger-ui/index.html`

Спецификацию в формате OpenAPI (YAML) можно скачать здесь:
`http://localhost:8080/v3/api-docs.yaml`

## 🔑 Тестовые данные

Для удобства тестирования в систему уже добавлены следующие учетные данные:

| Роль | Email | Пароль |
| :--- | :--- | :--- |
| **ADMIN** | `admin@example.com` | `admin123` |

*Новые пользователи могут зарегистрироваться через эндпоинт `/api/auth/register`.*

## 🧪 Тестирование

Запуск юнит-тестов:
```bash
./mvnw test
```
